# 新增分组健康标签页

## Summary
新增 Dashboard 标签页 `Group Health`，保留现有 `Group Analytics` 用量分析页。页面按“每个分组一个小面板”展示 24 小时整体成功率、倍率、供应商数量、平均首字延迟、平均延迟、平均 token/s，以及 24 小时内按 10 分钟窗口聚合的颗粒状态条。数据来源为真实请求 + 自动探针。

## Key Changes
- 后端新增 `GET /api/perf-metrics/groups?hours=24&interval_minutes=10`。
- 返回结构：
  - `groups[]`: `group`, `ratio`, `provider_count`, `request_count`, `success_rate`, `avg_ttft_ms`, `avg_latency_ms`, `avg_tps`, `buckets[]`
  - `buckets[]`: `ts`, `end_ts`, `request_count`, `success_count`, `success_rate`, `avg_ttft_ms`, `avg_latency_ms`, `avg_tps`, `status`
  - `status`: `empty | ok | warning | error`
- 成功率阈值：
  - `ok`: `>= 99%`
  - `warning`: `>= 95% && < 99%`
  - `error`: `< 95%`
  - `empty`: 该 10 分钟窗口无样本
- 分组倍率取 `ratio_setting.GetGroupRatioCopy()`；供应商数量按启用渠道分组去重统计，复用 provider-tree 语义：有 `provider_id` 按供应商去重，未归属渠道按独立供应商计数。
- 增加 `10min` 性能指标桶支持；新接口将 `minute`、`5min`、`10min` 数据归并到 10 分钟窗口。若历史数据是 `hour` 桶，只能按小时桶所在窗口展示，无法恢复更细历史粒度。
- 自动探针写入健康样本：在 `testIdleEnabledGroups` 的渠道测试结果完成后记录 `PerfMetricSample`，成功样本包含延迟、首字延迟、completion tokens；失败样本只计入请求数和失败数。手动渠道测试不写入该自动探针样本，避免人工测试污染健康条。
- 前端新增 `new_web` Dashboard section：`group-health`，标题 `Group Health`，仅 admin 可见。
- UI 使用现有 shadcn/base-ui 组件：
  - 卡片面板使用 `rounded-lg border bg-card`
  - 倍率和供应商数用 `Badge`
  - 10 分钟颗粒条用 `Tooltip` 展示窗口时间、成功率、首字延迟、延迟、token/s、请求数
  - 加载用 `Skeleton`，空状态显示无性能数据
- 增加相关 i18n 文案到 `new_web/src/i18n/locales/*`。

## Test Plan
- Go 单测：
  - 分组汇总能跨模型聚合成功率、TTFT、延迟、TPS。
  - pending metrics 未 flush 时也会出现在新接口。
  - 1/5/10 分钟桶正确归并到 10 分钟窗口。
  - 空窗口返回 `empty`，失败率阈值返回正确 `status`。
  - 供应商数量按启用渠道与多分组渠道正确去重。
  - 自动探针成功/失败会写入性能样本，手动测试不写入。
- 前端校验：
  - `bun run build`
  - 页面在桌面三列、小屏单列下无文本重叠。
  - Tooltip 可查看 10 分钟窗口详情。

## Assumptions
- “分组整个成功率”定义为该分组 24 小时内所有模型、所有启用渠道真实请求与自动探针样本的整体成功率。
- 平均延迟、首字延迟、token/s 只基于成功样本计算；失败样本只影响成功率。
- 现有历史 `hour` 粒度数据不会被重算；启用 `10min` 后的新数据才完整支持 10 分钟颗粒展示。
